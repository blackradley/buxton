%h2 Re-order Strategies
- if @strategies.length == 0
  %p
    There are no strategies in this organisation yet.
    = link_to 'Please add some', { :action => 'new', :id => @organisation.id }
- else
  %p Drag the strategies to rearrange them.
  %ul#sortable_strategies
    - for strategy in @strategies
      %li.strategy{:id=>"strategy_#{strategy.id}"}
        %span
          =h strategy.description
  
  = sortable_element('sortable_strategies', :url => {:action => 'update_strategy_order'})

%span.button
  = link_to 'Back', organisation_strategies_url(@organisation)