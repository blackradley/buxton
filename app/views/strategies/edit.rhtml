<h1>Editing <%= @organisation.name -%> Strategy</h1>

<% form_for [@organisation, @strategy] do |f| %>
  <%= render :partial => 'form' %>
  <span class="button">
	  <%= submit_tag 'Save' %>
  </span>
<% end %>

<%= link_to 'Show', organisation_strategy_url(@organisation, @strategy) -%> | <%= link_to 'Back', organisation_strategies_url(@organisation) -%>
